# Code generated by kickr; DO NOT EDIT.

---
include:
  - local: .gitlab/pipelines/*.yml

# secret variables
# (define the variables below in your GitLab group/project variables)

# NETLIFY_SITE_ID: The netlify site ID where builds will be published (should be retrieved if the project name is the same here https://app.netlify.com/sites/website/configuration/general)
# NETLIFY_AUTH_TOKEN: The netlify authentication token (it's a personal token) to use for deployments (once connected, can be retrieved here https://app.netlify.com/user/applications#content)

default:
  interruptible: true

variables:
  PROD_REF: /^(main|master|v[0-9]+(\.[0-9]+)?\.x)$/
  INTEG_REF: /^(alpha|beta|dev[a-z]*|next|rc|staging)$/
